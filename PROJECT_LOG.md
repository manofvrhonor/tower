# 📋 PROJECT LOG — TOWER OF TIME

> При старте новой сессии — приложить целиком вместе с `AGENTS.md` и `CURRENT_TASK.md`.
> Свежие события — внизу разделов.

---

## 🎯 ГЛАВНАЯ ЦЕЛЬ

VR-игра **Tower of Time** для Meta Quest 3 в браузере (WebXR), по постоянной ссылке.

### Геймплей

Игрок в белой комнате 3×3×3 м. В центре — стол-пьедестал с прозрачным куполом. Вокруг в невесомости плавают:
- **цветные кубики** — для сборки башни по схеме на столе;
- **серые мусорные кубики** — визуальный шум (`isTarget: false`);
- **красные светящиеся шары с трейлами** — летают по траекториям, опасны.

### Купол

- Плавающие кубики отскакивают от купола снаружи.
- Рука с кубиком свободно проходит сквозь.
- Отпустил кубик внутри → становится физическим, падает на стол.
- Отпустил снаружи → летит в невесомости с импульсом руки.
- Скатился со стола, коснулся пола → возвращается в плавающий режим.
- Красные шары проходят сквозь купол, могут сбить башню.

### Замедление времени (как в SUPERHOT)

- Игрок замер → `timeScale ≈ 0.05`.
- Двигается → `timeScale = 1.0`.
- Применяется к скриптовому движению (плавающие кубики, шары).
- Физика рук, физика на столе, сам игрок — реальное время.

### Отбивание

В комнате — бита/сковородка/ракетка/рыба, ими отбиваются красные шары.

### Победа / поражение

- **Победа:** башня собрана по схеме, неподвижна несколько секунд.
- **Поражение:** красный шар коснулся капсулы тела игрока.

### MVP

Друг надевает шлем без объяснений, проходит за 2–5 минут, говорит «прикольно».

### Таймлайн

~1 неделя.

### Не-цели

Уровни, меню, сохранения, графика AAA, мультиплеер, не-VR версии.

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Основа

- **A-Frame 1.5.0** — `https://cdn.jsdelivr.net/npm/aframe@1.5.0/dist/aframe-master.min.js`
- **Three.js r158** — внутри A-Frame.
- **WebXR API** — обёрнут A-Frame.

### Компоненты

- **`@c-frame/physx`** — единый физический движок и система захвата. В комплекте `physx-grabbable` и `hand-grab`. Подключается на шаге 1.3 (Сессия 5). Версия и точная CDN-ссылка определяются в начале Сессии 5 (шаг A текущей задачи).
- **`hand-controls`** (встроенный в A-Frame) — отображение и трекинг рук. Подключён на шаге 1.2, с локальными GLB-моделями из `assets/models/`. На шаге 1.3 на entity рук добавятся `physx-body="type: kinematic"` + `hand-grab`.
- **aframe-extras** — по необходимости.

### Подход

- Без сборщиков (нет Webpack/Vite/etc).
- Чистый HTML + ES-модули. Без TypeScript. Без React/Vue/Svelte.
- Все библиотеки — `<script src="https://cdn.jsdelivr.net/...">`.
- Один компонент = один файл в `js/components/`.
- Геймплейные числа — в `js/config.js`.
- Git — единственный источник версий. Каждый микро-шаг = коммит в `main`. Откаты — через `git`. `git push` — пакетом в конце сессии или для бэкапа.

### Не используем

❌ Vite / Webpack / Parcel
❌ TypeScript
❌ React / Vue / Svelte
❌ npm-зависимости проекта
❌ Unity / Godot / Unreal
❌ готовые 3D-модели на старте

### Отменено по ходу проекта

- ~~`aframe-physics-system@4.0.2`~~ — заброшен, несовместим с Three.js r125+.
- ~~`@c-frame/aframe-physics-system@4.2.2`~~ (Cannon) — отменён в Сессии 5, см. «Разворот с Cannon на PhysX».
- ~~`super-hands`~~ — не регистрируется в `AFRAME.components`, отменён в Сессии 5.
- ~~Vite + TypeScript + React~~ — слишком тяжело для VR-прототипа на неделю.
- ~~Авто-деплой Netlify ← `main`~~ — каждый коммит запускал платный деплой. Сейчас деплой ручной.

---

## 💻 ОКРУЖЕНИЕ

| Параметр | Значение |
|---|---|
| ОС | Windows |
| Терминал | **PowerShell** |
| VR | Meta Quest 3 (Quest Link для тестов) |
| Папка проекта | `D:\VIBECODING\Tower` |
| Редактор | VS Code |
| Локальный сервер | `npx serve` |
| Десктоп-отладка | Chrome + WebXR API Emulator |
| Браузер в шлеме | Meta Quest Browser |

---

## 🏗 АРХИТЕКТУРНЫЕ РЕШЕНИЯ

### CDN — только jsDelivr

Все внешние библиотеки — через `https://cdn.jsdelivr.net/`.
Не использовать: `aframe.io`, `unpkg.com`, `cdn.aframe.io`, любые другие CDN.
Причина: стабильность из РФ без VPN, единая точка отказа.

### Стратегия тестирования — локально, не Netlify

- Цикл разработки: правка → сохранить → `npx serve` → перезагрузить вкладку Chrome → при необходимости войти в VR через Quest Link.
- **Quest Link + localhost.** Quest 3 подключён к ПК через Quest Link. В десктопном Chrome на `http://localhost:<порт>` иммерсивный VR работает напрямую — шлем показывает то, что в браузере.
- Деплой Netlify — только в конце этапа или по явному запросу пользователя.
- Публичная HTTPS-ссылка нужна, только если тестируем в Quest Browser напрямую (без Quest Link). Тогда — деплой по запросу.

### Git и откаты

- Каждый микро-шаг = отдельный коммит в `main`.
- Откаты — через `git reset` / `git checkout`, не через Netlify rollback.
- `git push` — пакетом в конце сессии или для бэкапа, не после каждого коммита.

### Разворот с Cannon на PhysX (Сессия 5)

**Что было.** Шаг 1.1 закрыт на `@c-frame/aframe-physics-system@4.2.2` (Cannon). На шаге 1.3 предполагалось подключить `super-hands` для захвата.

**Что произошло.** В Сессии 4 `super-hands` не регистрируется в `AFRAME.components` ни в одном из вариантов подключения через jsDelivr. Без `super-hands` Cannon-стек не даёт захват.

**Что решено.** Перейти на `@c-frame/physx`. Это единый движок: физика тел + встроенный захват (`physx-grabbable` + `hand-grab`). Отдельная библиотека уровня super-hands не нужна.

**Последствия.**
- Шаг 1.1 (Cannon) будет переделан на PhysX в шагах F–G задачи 1.3.
- Cannon и super-hands из проекта удаляются. Возврат — только по явному запросу пользователя.
- Перед миграцией основной сцены — отдельная песочница `sandbox-physx.html`. До успеха в песочнице (десктоп + Quest 3) `index.html` не трогаем.

**Риски.**
- PhysX тянет `.wasm`-файл. Если со стороннего CDN, недоступного из РФ — фоллбэк: положить `.wasm` локально в `assets/`.
- Совместимость `hand-controls` + `physx-body="type: kinematic"` на одном entity не проверена. Если конфликт — разделить визуализацию руки и физическое тело руки на разные entity.

---

## ✅ ЧТО СДЕЛАНО

### Этап 0 — Базовая сцена ✅

Сцена A-Frame 1.5.0: белая комната 3×3×3 м, пьедестал в центре (цилиндр R=0.3 м, H=1 м). Работает локально, на Netlify, в Quest 3.

### Этап 1 — Физика и руки

- **Шаг 1.1** ✅ (Cannon-версия). Пол и пьедестал — `static-body`. Тестовый красный кубик 10×10×10 см с `dynamic-body="mass: 1"` падает на пьедестал.
  *Будет переделан на PhysX в шагах F–G задачи 1.3.*
- **Шаг 1.2** ✅. Оба entity рук — внутри rig'а `<a-entity id="player">`. На каждой руке: `hand-controls` + `gltf-model` с локальной GLB из `assets/models/`. В Quest 3 руки видны и трекаются.
- **Шаг 1.3** 🔄 в работе — `CURRENT_TASK.md`. Захват кубика руками через `@c-frame/physx`.

### Этапы 2+ — пока не начаты

Купол, плавающие кубики, замедление времени, красные шары, отбивание, башня, победа/поражение.

---

## 📍 ГДЕ МЫ СЕЙЧАС

- **Активная задача:** шаг 1.3 — захват кубика через `@c-frame/physx`. Полный план — `CURRENT_TASK.md`.
- **Текущий микро-шаг:** A — определить актуальную версию `@c-frame/physx` и собрать jsDelivr-ссылку.
- **Стек физики:** в процессе миграции с Cannon (шаг 1.1) на PhysX (шаг 1.3, в работе).
- **Захват:** ещё не работает. Будет реализован через `physx-grabbable` + `hand-grab`.
- **Деплой:** ручной, по запросу. Текущая работа — на `localhost` через Quest Link.

---

## 🚦 СЛЕДУЮЩИЙ ШАГ

Шаг A задачи 1.3 — версия и CDN `@c-frame/physx`. Подробности и точная процедура — в `CURRENT_TASK.md`, раздел «Следующее действие».

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### VR-руки иногда не отображаются без VPN

Симптом: `hand-controls` не показывает дефолтную модель руки. Решение — локальные GLB в `assets/models/leftHandLow.glb` и `rightHandLow.glb`, подключаются через `gltf-model`. Сейчас работает стабильно.

При переходе на PhysX (шаг 1.3) проблема измениться не должна — она про модель руки, не про физику. Если изменится — фиксируем как новый факт в `CURRENT_TASK.md`.

### `super-hands` не регистрируется в `AFRAME.components`

Сессия 4. Перепробованы разные варианты подключения через jsDelivr — компонент не появляется в реестре A-Frame. Причина разворота на PhysX. Не возвращаемся без явного запроса пользователя.

---

## 📜 ИСТОРИЯ СЕССИЙ

### Сессия 1 — Этап 0

Базовая сцена A-Frame 1.5.0, комната 3×3×3 м, пьедестал, проверка в Quest 3. Деплой на Netlify.

### Сессия 2 — Шаг 1.1

Подключён `@c-frame/aframe-physics-system@4.2.2` (Cannon). Пол и пьедестал — `static-body`, тестовый кубик — `dynamic-body="mass: 1"`. Кубик падает на пьедестал.

### Сессия 3 — Шаг 1.2

`hand-controls` + локальные GLB-модели рук в `assets/models/`. Руки видны и трекаются в Quest 3. Решена проблема «руки без VPN».

### Сессия 4 — Попытка super-hands

Подключение `super-hands` через jsDelivr. Компонент не регистрируется в `AFRAME.components` ни в одном варианте. Принято решение свернуть направление, искать альтернативу.

### Сессия 5 — Разворот на PhysX (в работе)

Решение: перейти на `@c-frame/physx` (физика + захват в одном пакете). Cannon и super-hands отменены. План шага 1.3 переписан под PhysX. Текущий микро-шаг — A (версия и CDN).

---

## 📂 СТРУКТУРА ПРОЕКТА

~~~
D:\VIBECODING\Tower\
├── index.html              — основная сцена
├── sandbox-physx.html      — песочница PhysX (создаётся в шаге B задачи 1.3)
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── config.js           — геймплейные числа
│   └── components/         — по одному компоненту на файл
├── assets/
│   ├── models/
│   │   ├── leftHandLow.glb
│   │   └── rightHandLow.glb
│   └── (возможно physx.wasm локально, если CDN не доступен)
├── AGENTS.md               — правила агента
├── PROJECT_LOG.md          — этот файл
└── CURRENT_TASK.md         — текущая задача
~~~