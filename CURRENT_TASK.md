---

name: Current Task

alwaysApply: true

---

# CURRENT_TASK.md — Текущая задача

## Задача: Фаза 3 — слои мира (outside-scenery)

**Мастер-план:** `.cursor/plans/tower_stylish_game_c39f4c3b.plan.md` (фазы 0–7).

**Цель:** за большим cyan-куполом видны силуэты «города» — глубина сцены как на референсе.
Первый шаг — заглушки-боксы по кругу, без GLB.

**Критерий завершения (шаг 3.1):** в VR/Chrome видны несколько тёмных «домов» за куполом,
не пересекают игровую зону, не ломают физику; параметры в `CONFIG`.

### Микро-шаги

- ⬜ **3.1 — outside-scenery.js:** компонент + `<script>` в `index.html`; боксы по окружности
  (`radius`, `count`, `height`, `color`) из config; без коллайдеров.
- ⬜ **ПК QA:** дома видны снаружи купола, не клипятся в камеру внутри.
- ⬜ **Quest QA:** глубина читается в шлеме.

### Альтернатива (если решишь иначе)

- **2.x — малый купол над ядром:** убрать визуал / оставить зону сборки / рестайл —
  отдельная задача по подтверждению (можно вставить перед 3.1).

## Working Context

### ИЗВЕСТНО

- Фаза 0 ✅, Фаза 1 (снеп + победа) ✅ Quest QA.
- Фаза 2.1 ✅ — cyan-купол (`room-fog-dome.js`).
- **VR-меню layout ✅** (с.38): `victory-ui` — panel h 0.50, кнопки подняты; Quest QA
  старт-меню + победа — OK.
- HDR уже есть: `world-hdri-sky`, `assets/hdri/city.jpg`.

### НЕИЗВЕСТНО

- Оптимальный `radius`/`count` домов для Quest (начать с 8–12 заглушек).
- Нужен ли туман по полу (3.2) сразу после 3.1 или отдельным шагом.

### РЕШЕНО

- Layout game-menu/victory-ui **не трогаем** — Quest QA пройден.
- Фаза 3 идёт **до** решения по малому куполу (2.x), пока пользователь не попросит иначе.

## Файлы

- **Трогаем:** новый `js/components/outside-scenery.js`, `js/config.js`, `index.html`.
- **Не трогаем:** физика, assembly, меню, room-fog-dome (кроме согласования радиуса).

## Следующее действие

Добавить `CONFIG.room.outsideScenery` + компонент `outside-scenery` — кольцо box-примитивов
за `fogDome.radius`.
