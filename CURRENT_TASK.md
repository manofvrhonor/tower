---

name: Current Task

alwaysApply: true

---

# CURRENT_TASK.md — Текущая задача

## Задача на следующую сессию не выбрана

**Этап 6** в работе (Quest QA не закрыт). **Этап 7** закрыт: захват биты (сессия 20)
и отбивание шара (сессия 21, clamp-окно) работают, Quest QA ✅.
**Полировка (сессия 22):** коллайдеры комнаты `a-plane → a-box` (ADR-17), рассинхрон
устранён, десктоп QA ✅.

**Задача на следующую сессию ещё не выбрана** — взять один пункт из бэклога.

---

## Бэклог (приоритеты на следующие сессии)

> Заведено в сессии 20, обновлено в 22. Порядок — примерный, уточнить при старте.

1. ✅ **Скорость шара при отбивании битой** — починено (сессия 21): clamp-окно
   `_clampBatDeflect` в `red-ball.js`, `CONFIG.balls.batDeflect.clampMs`.

2. ✅ **Рассинхрон видимой и физической комнаты** — починено (сессия 22, ADR-17):
   стены/пол/потолок переведены с `a-plane` на `a-box` (`index.html`).

3. **Меню входа + выбор уровней сложности.**
   Стартовый экран (вход в VR), выбор сложности (влияет на число/скорость шаров и т.п.).
   Решить: отдельная сцена/оверлей; как параметризовать `init-session.js` / `CONFIG`.

4. **Текстуры, визуальные эффекты, полировка** (этап 8).

5. **Уменьшить размер шаров ~×2.** `CONFIG.balls` (радиус/геометрия + trail-профиль).

6. **Руки «как магниты».**
   Сейчас объект «прилипает» к руке при контакте, нет хвата пальцами. Изучить хват
   живыми пальцами (hand-tracking) вместо контроллеров; пересмотреть `physx-grab`
   (порог/жест захвата, точка крепления).

7. **Quest QA этапа 6/7** (перенос): пьедестал/сбивание башни/бита в шлеме.

---

## Не закрытый QA (перенос с этапа 6/7)

- [ ] **Бита (десктоп):** F5 → grip на ручку/кончик → замах → отпуск → на пьедестале.
- [ ] **Бита (Quest):** захват и отбивание в шлеме.
- [ ] **Шары 4a.** Пьедестал — шар отскакивает, не проходит (Quest).
- [ ] **Шары 4c.** Шары валят башню (Quest).
- [ ] **Шары 5.** Полный Quest QA этапа 6.

---

## Working Context

### ИЗВЕСТНО

- Шары: mass 2.0, restitution 0.32, `cubeHitImpulseMultiplier` 2.8, homing-циклы (0/1/2 отскока).
- Стены/пьедестал: `collidesWithLayers` включает BALL (5).
- Бита: kinematic через state `grabbed` на physx-body (не ручной flag); блин+ручка — дочерние entity.

### РЕШЕНО (сессия 20–22)

- Захват биты: `grabbed` state → physx-body `setKinematicTarget` (мировой pose), parenting к руке.
- Захват по всей бите: без `geometry` на корне; блин и ручка — дочерние → коллайдер на каждом.
- Откат регрессии: `_touchEl`/early-grab убраны из `physx-grab` (ломали захват кубиков).
- Отбивание шара: clamp-окно `_clampBatDeflect` (`red-ball.js`) держит скорость на
  доударной `CONFIG.balls.batDeflect.clampMs` мс — одного сброса по `contactbegin` мало
  (kinematic-бита разгоняет шар несколько кадров). Quest QA ✅.
- Комната: коллайдеры стен/пола/потолка `a-plane → a-box` (ADR-17). `a-plane` строит
  ненадёжный convex hull (коллайдер уезжает внутрь на 15–20 см); `box` = точный
  `PxBoxGeometry`. Десктоп QA ✅.

### Файлы

**Не трогать без нужды:** `floating-cube.js`, victory-*, `ball-bat.js`, `red-ball.js`,
коллайдеры комнаты в `index.html`.

---

## Следующее действие

Выбрать с пользователем следующий пункт бэклога (№3 меню/сложность, №5 размер шаров,
№6 руки-пальцы, №4 текстуры или №7 Quest QA).
