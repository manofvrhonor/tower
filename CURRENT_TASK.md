---

name: Current Task

alwaysApply: true

---

# CURRENT_TASK.md — Текущая задача

## Задача: комната-купол + skybox (сессия 30)

**Приоритет:** визуальное окружение — комната как купол/сфера, skybox вместо однотонного фона.

**Предыдущая (с.29):** меню входа + режимы сложности — ✅ закрыта, Quest QA OK (ADR-19).

---

## Цель и критерий завершения

**Сделано**, когда в Quest + desktop:

1. Комната выглядит как **купол/сфера** (или согласованный вариант), без регрессии PhysX-коллайдеров.
2. **Skybox** вместо `background color` — иммерсивное небо/окружение.
3. Стол, купол игры, кубы, UI — читаемы на новом фоне.
4. Регрессия: меню, сложности, захват, slo-mo, победа.

---

## Микро-шаги

- [ ] **0 — Образ и референс:** что именно рисуем (купол комнаты vs skybox-only); не трогаем коллайдеры без нужды.
- [ ] **1 — Skybox:** `<a-sky>` или env map; параметры в `config.js`; убрать/заменить `background color`.
- [ ] **2 — Визуал комнаты:** стены/потолок — купол или упрощённый вариант; **физика** (a-box) без изменений на первом шаге.
- [ ] **3 — Quest QA** — читаемость, FPS, регрессия.

---

## Файлы

| Трогаем | Не трогаем |
|---|---|
| `index.html` | `physx-grab.js` |
| `config.js` | `dome-builder.js` (физика купола игры) |
| `css/style.css` (если нужно) | `game-lifecycle.js`, меню |
| новый компонент комнаты (если нужен) | collision layers |

---

## Подводные камни

- PhysX-коллайдеры комнаты (a-box) **не** менять ради красоты — только визуал поверх/вместо mesh.
- Skybox + прозрачный купол игры — проверить z-fighting и читаемость ghost-wireframe.
- Текстуры — локальные файлы в `assets/`, не внешние CDN кроме jsDelivr при необходимости.

---

## Working Context

### ИЗВЕСТНО

- Комната сейчас: 6× `a-box` (пол, потолок, 4 стены) + однотонный `background`.
- Игровой купол — отдельная сущность (`dome-visual` + `dome-collider`).

### НЕИЗВЕСТНО

- Skybox: процедурный цвет / одна equirectangular / cubemap — решить на шаге 0.

### РЕШЕНО (не переоткрывать)

- Меню, сложности, ghost wireframe, прицел UI — ADR-19.
- **QA = только Quest 3** (Quest Link + localhost). ПК — `npx serve`, консоль, правки кода; не геймплей-тест.

---

## Бэклог (после этой задачи)

- Подогнать позицию меню в VR (`CONFIG.game.menu.worldPosition`).

---

## Следующее действие

**Микро-шаг 0:** согласовать визуальный образ (купол комнаты + skybox) перед кодом.
