---
name: Current Task
alwaysApply: true
---

# CURRENT_TASK.md — Текущая задача

## Задача: Фаза 4 — локации

**Статус:** переделка сборки (с.57–61) **✅ закрыта** — commit + push main.

### Закрыто (с.61)

- ✅ Сегменты колец `machine-ring-collider.js` (outer 0.34 / inner 0.30).
- ✅ `victory-freeze.js` — пауза мира на победе.
- ✅ Clamp спавна по `_COL` + разведение / отложенный импульс.
- ✅ Quest QA ПК + Quest.
- ✅ `coreSpinAxis` — пользователь.

### Следующая сессия

1. **Фаза 4** — локации (перенос деталей между комнатами, fogTint).
2. Смотреть `PROJECT_START.md` ARCHIVE + `CONFIG.locations`.

**Не делать:** convex `_COL.glb` на кольцах (DECISIONS LOCK, с.60).

---

## Старт сессии

1. Прочитать `PROJECT_START.md` → «Где мы», Фаза 4 в `PROJECT_LOG.md`.
2. Уточнить у пользователя: первая механика локаций (телепорт? двери?).
