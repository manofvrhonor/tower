---
name: Current Task
alwaysApply: true
---

# CURRENT_TASK.md — Текущая задача

## Задача 3 — Купол над столом

**Статус:** Шаги 1, 2, 3 закрыты в Сессии 8 (Шаг 3 — временным решением).
Следующая сессия начинается с **Шага 3.5** (система collision groups).
**Этап в `PROJECT_LOG.md`:** Этап 3 — Купол над столом.

---

## Цель (без изменений)

Над пьедесталом — прозрачный купол. Купол:

1. Видим, полупрозрачен, не мешает обзору. ✅
2. Удерживает плавающие кубики внутри своего объёма (отскакивают от стенок). ✅
3. «Прозрачен» для руки игрока: схваченный кубик можно проносить через
   стенку купола внутрь и наружу. ⚠️ работает временно (через
   фантомизацию всего кубика), будет правильно на Шаге 3.5.
4. Release **внутри** → кубик переключается в `state: 'gravity'` и падает
   на пьедестал. ⏳ Шаг 4
5. Release **снаружи** → кубик остаётся в `'float'`. ⏳ Шаг 4
6. Контакт `'gravity'`-кубика с полом → возврат в `'float'`. ⏳ Шаг 5

**Дополнительное требование, выявленное в Сессии 8:**
Схваченный кубик должен проходить сквозь **только купол**, но **сталкиваться
со всем остальным**: с другими кубиками (можно отбивать их как битой),
со столом, с полом, в будущем — с красными шарами. Это критично для
геймплея «кубики разных цветов = биты с разными свойствами».

## Критерий завершения

- ✅ Купол виден в Quest, не «глючит» при движении головы.
- ✅ Тест 1: бросить плавающий кубик в стенку купола изнутри — отскакивает.
- ⏳ **Тест 1.5 (новый):** схваченным кубиком ударить по другому кубику —
  отбивается, не проходит насквозь.
- ⏳ Тест 2: схватить снаружи, протащить руку через стенку, отпустить
  внутри — упал на пьедестал, не отскакивает.
- ⏳ Тест 3: схватить с пьедестала, вытащить наружу, отпустить — снова
  плавает.
- ⏳ Тест 4: собрать башню, ронять; кубики на столе лежат под гравитацией;
  кубики, выкатившиеся за край, падают на пол и возвращаются в облако.
- ✅ В консоли нет ошибок PhysX.

---

## Состояние реализации (что уже работает)

### Геометрия купола — реализована
Цилиндр `a-cylinder` open-ended Y=1.00…1.30, центр (0, 1.15, 0), радиус
0.27 + полусфера `a-sphere` theta-length 90°, центр (0, 1.30, 0), радиус
0.27. Параметры в `CONFIG.dome`.

### Физика купола — 89 плиток
**Отклонение от плана.** Вместо двух static-тел PhysX купол собран из 89
мелких static-плиток-кубиков, аппроксимирующих стенку и крышку (плотно,
без щелей). Причина: PhysX/A-Frame на open-ended цилиндре и полусфере не
строит точный shape. FPS в Quest в порядке.

### Прозрачность для руки — временное решение (per-cube фантомизация)
**Отклонение от плана.** Файл `dome-permeable.js` не создавался. В
`js/components/physx-grab.js` при grab у схваченного кубика снимается
`eSIMULATION_SHAPE` со всех shape'ов; при release — возвращается.

**Известный недостаток:** кубик в руке проходит сквозь **всё**, не только
сквозь купол. Поэтому им нельзя отбивать другие кубики. Чинится Шагом 3.5.

Используемый API (зафиксировано в `PROJECT_LOG.md` Сессия 8):
- `el.components['physx-body'].shapes` — массив `PxShape`.
- `sceneEl.systems.physx.PhysX.PxShapeFlag.eSIMULATION_SHAPE` — флаг.
- `shape.setFlag(flag, boolean)` — работает «на лету».

---

## Архитектурные решения, ревизия после Сессии 8

### Решение 5 (новое). Collision groups вместо фантомизации

Реализуем точечную фильтрацию коллизий через `PxFilterData`. Вводим
реестр групп **на весь проект**, а не только под купол, потому что в
Этапе 6 (красные шары) и при добавлении цветных свойств кубиков
фильтрация понадобится снова.

**Реестр групп (черновик, может уточниться после чтения исходника
`@c-frame/physx@v0.3.0`):**

| Группа | Назначение | Сталкивается с |
|---|---|---|
| `WORLD` | пол, стол, пьедестал | всем |
| `DOME` | 89 плиток купола | всеми кубиками **кроме** `GRABBED_CUBE`; шары — TBD на Этапе 6 |
| `FLOAT_CUBE` | плавающий кубик (state: float) | WORLD, DOME, FLOAT_CUBE, GRAVITY_CUBE, GRABBED_CUBE, BALL |
| `GRAVITY_CUBE` | кубик в режиме гравитации | то же, что FLOAT_CUBE |
| `GRABBED_CUBE` | кубик в руке | WORLD, FLOAT_CUBE, GRAVITY_CUBE, BALL — **но не DOME** |
| `BALL` | будущие красные шары (Этап 6) | резерв |
| `HAND` | контроллеры | TBD |

Ключевая отключаемая пара: **`GRABBED_CUBE × DOME`**.

При grab: группа кубика `FLOAT_CUBE` (или `GRAVITY_CUBE`) → `GRABBED_CUBE`.
При release: возвращается прежняя группа (или новая по итогу
containment-теста на Шаге 4).

Реестр групп выносится в **`CONFIG.collisionGroups`**. Шары и цвета
кубиков из будущих этапов лягут поверх этой схемы как подгруппы (через
word1 в PxFilterData), не ломая базовую логику.

### Решения 1–4 — без изменений

Решения 1, 3, 4 (геометрия купола из плиток, containment-тест,
возврат в float по полу) остаются в силе.
Решение 2 (фантомизация купола / кубика) **отменено и заменено
Решением 5**.

---

## Оставшиеся микро-шаги

### Шаг 3.5. Collision groups ← **следующий, начало Сессии 9**

#### Шаг 3.5.A. Разведка API (без правки кода)

Прочитать исходник `@c-frame/physx@v0.3.0` и выяснить:

1. Принимает ли `physx-body` атрибут вида `collisionGroup` /
   `collisionMask` / `collision-filter-group` / `collision-filter-mask`.
2. Где задаётся фильтр-шейдер (PxSimulationFilterShader) — в системе или
   нет; учитывает ли он word0/word1 PxFilterData по стандартной формуле
   `(word0 & otherMask) && (otherWord0 & ourMask)`.
3. Доступ ли `shape.setSimulationFilterData(PxFilterData)` через обёртку.
4. Есть ли системный helper типа `setGroupCollision(a, b, bool)`.

Возможные исходы:
- **Сценарий 1 (повезло):** атрибуты есть, фильтр стандартный → правка
  маленькая.
- **Сценарий 2 (типичный):** есть `body.shapes` и raw API
  `setSimulationFilterData`, фильтр стандартный → пишем helper.
- **Сценарий 3 (плохой):** фильтр-шейдер хардкодный/непригодный.
  Варианты: (а) патч компонента при инициализации, (б) откат на
  «зональную фантомизацию» (фантомизировать кубик только когда его центр
  в кольцевой зоне у стенки купола, по tick).

Результат разведки фиксируется в `PROJECT_LOG.md` Сессии 9.

#### Шаг 3.5.B. Реализация (минимум для купола, с заделом)

- В `js/config.js` появляется `CONFIG.collisionGroups` со всеми именами
  групп из реестра выше — даже если шары/цвета пока не используются.
- На 89 плиток купола проставляется группа `DOME`.
- На пол, стол, пьедестал — `WORLD`.
- На плавающие кубики — `FLOAT_CUBE`.
- В `js/components/physx-grab.js`:
  - убирается текущий `_setSimulationShape` (фантомизация);
  - при grab — смена группы кубика на `GRABBED_CUBE`;
  - при release — возврат на `FLOAT_CUBE` (на Шаге 4 это уточнится:
    `GRAVITY_CUBE` если внутри купола, `FLOAT_CUBE` если снаружи).
- Конфигурация фильтра (что с чем сталкивается) задаётся либо
  декларативно через `CONFIG.collisionGroups`, либо вызовом
  `setGroupCollision(GRABBED_CUBE, DOME, false)` при инициализации сцены —
  зависит от исхода Шага 3.5.A.

#### Шаг 3.5.C. Проверка
- Тест 1.5: схваченным кубиком ударить по другому кубику — отбивается.
- Тест «сквозь купол»: кубик в руке свободно проходит стенку.
- Тест: схваченный кубик не проходит сквозь стол/пол.
- Стабильность после серии grab/release.

### Шаг 4. Release-inside → gravity

- В `floating-cube.js` — хук на release кубика.
- Containment-тест по Решению 3 (см. ниже, без изменений).
- Внутри → `state = 'gravity'`: снять `eDISABLE_GRAVITY`, выставить
  «земные» damping, **сменить collision group на `GRAVITY_CUBE`**.
- Снаружи → ничего не меняем (кроме того, что Шаг 3.5 вернёт
  `FLOAT_CUBE`).
- Проверка: тесты 2 и 3.

### Шаг 5. Возврат в float по полу

- Присвоить полу `id="floor"` в `index.html`.
- Подписка `contactbegin` в `floating-cube.js`. Если `state === 'gravity'`
  и партнёр — `#floor` → `'float'`: вернуть `eDISABLE_GRAVITY`,
  восстановить float damping, маленький импульс вверх, **сменить группу
  на `FLOAT_CUBE`**.
- Контакт со столом возврат **не** триггерит.
- Проверка: тест 4.

### Шаг 6. QA-прогон на Quest

- Все 5 тестов (включая 1.5), стабильность фильтрации после серии
  grab’ов, FPS, устойчивость башни.

---

## Containment-тест (Решение 3, без изменений) — справочник для Шага 4

Центр кубика проверяется «внутри капсулы»:
- проекция Y на ось:
  - `Y ∈ [wallBottomY, wallTopY]` (1.00…1.30) — внутри, нужно
    `dx² + dz² ≤ (R − halfCube)²`;
  - `Y > wallTopY` — расстояние до полюса `(0, wallTopY, 0)` должно быть
    `≤ R − halfCube`;
  - `Y < wallBottomY` — снаружи.
- epsilon `0.01` для границы.

`halfCube = CONFIG.floatingCubes.size / 2` (= 0.05), `R`, `wallBottomY`,
`wallTopY` — из `CONFIG.dome`.

---

## Working Context

- `js/config.js` — `CONFIG.dome` (готов); **добавить
  `CONFIG.collisionGroups` на Шаге 3.5.B**.
- `js/components/physx-grab.js` — на Шаге 3.5.B убрать фантомизацию,
  добавить смену группы при grab/release.
- `js/components/floating-cube.js` — главный файл Шагов 4–5:
  release-логика, containment-тест, контакт с полом, переключение state и
  collision group.
- `index.html` — на Шаге 3.5.B проставить группы на пол/стол/пьедестал/
  плитки купола; на Шаге 5 — `id="floor"`.
- `node_modules/@c-frame/physx/...` (или CDN-источник) — **только чтение**
  на Шаге 3.5.A: API filter data, фильтр-шейдер, имена событий.

---

## Риски на оставшиеся шаги

| Риск | Вероятность | План Б |
|---|---|---|
| Фильтр-шейдер в `c-frame/physx` не учитывает наши word0/word1 (Сценарий 3 на Шаге 3.5.A) | средняя | (а) патч компонента; (б) «зональная фантомизация»: снимать `eSIMULATION_SHAPE` только когда центр кубика в кольцевой зоне стенки. |
| `setSimulationFilterData` в обёртке не экспортирован | средняя | Идти через `body.shapes[i].setSimulationFilterData(...)` напрямую с raw `PX.PxFilterData(...)`. |
| Имя события release в `physx-grab` неочевидно (для Шага 4) | низкая | Прочитать исходник; альтернатива — слушать `gripup`/`triggerup` на контроллере и определять «свой» кубик через `hitEl`. |
| Кубик «застревает» ровно на границе купола при release (Шаг 4) | низкая | epsilon `0.01`; маленький импульс к центру стола. |
| Контакт `gravity`-кубика с #floor не ловится (Шаг 5) | средняя | Проверить `physx-body` пола; иначе ловить по нормали контакта. |

---

## Что НЕ входит в эту задачу

- Красные шары и взаимодействие с куполом (Этап 6) — но реестр групп
  закладывается уже сейчас.
- Цветные свойства кубиков — реестр групп закладывается, поведение нет.
- Персистентность башни.
- Звуки удара.
- Анимация появления/исчезновения купола.