/**
 * config.js — все геймплейные параметры в одном месте.
 *
 * Правило проекта: если хочется что-то поменять (размер, цвет, скорость) —
 * сначала проверь, нет ли этого здесь. Магические числа в коде запрещены.
 */

const CONFIG = {
  // === Комната ===
  room: {
    width: 3,        // метры (X)
    depth: 3,        // метры (Z)
    height: 3,       // метры (Y) — куб 3×3×3
    floorColor:   '#8a8580',
    wallColor:    '#d4cfc0',
    ceilingColor: '#f0ede5',
  },

  // === Пьедестал в центре ===
  pedestal: {
    radius: 0.3,     // метры (диаметр 60 см)
    height: 1.0,     // метры
    color: '#3a3a3a',
  },

  // === Игрок ===
  player: {
    startPosition: { x: 0, y: 0, z: 1 },  // 1 метр южнее центра
    eyeHeight: 1.6,                        // высота камеры над полом игрока
  },

    // Плавающие кубики (свойство float — невесомость + инерция).
  // Используются компонентом floating-cube. См. CURRENT_TASK.md, задача 2.2.
  floatingCubes: {
    size: 0.1,                  // ребро куба, м
    color: '#4A90E2',           // синий, отличим от тестового красного

    // Физика парения
    disableGravity: true,       // отключить гравитацию для тела
    linearDamping: 0.1,         // вязкость линейного движения (0 — бесконечный разгон)
    angularDamping: 0.1,        // вязкость вращения

    // Стартовый импульс при спавне (направление случайное, скорость фиксированная)
    initialImpulseSpeed: 0.2,   // м/с

    // Точка спавна одиночного кубика на отладочном этапе (Шаг 3)
    spawnPosition: { x: 0, y: 1.7, z: 0 },
  },

};

// Делаем CONFIG доступным глобально, чтобы любой компонент мог его использовать.
// (Простое решение для проекта без сборщика. Для большого продакшена использовали бы модули.)
window.CONFIG = CONFIG;